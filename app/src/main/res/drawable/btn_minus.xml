<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- button background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/btn_bg_duration" />
            <corners android:radius="@dimen/btn_corner" />
        </shape>
    </item>
    <!-- Minus button -->
    <item
        android:drawable="@drawable/minus"
        android:left="12dp"
        android:top="8dp"/>
</layer-list>